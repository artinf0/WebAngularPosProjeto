import { ValidasenhaDirective } from './validasenha.directive';

describe('ValidasenhaDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidasenhaDirective();
    expect(directive).toBeTruthy();
  });
});
